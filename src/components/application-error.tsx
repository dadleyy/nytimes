import * as React from "react";

const ApplicationError : React.SFC = function(props : any) : JSX.Element {
  return (
    <section className="width-10 text-center padding-tb-4">
      <p>Something went wrong</p>
    </section>
  );
};

export default ApplicationError;
